#include <iostream>

using namespace std;

int main()

{
	/* Logische Operatoren: 

	   if ((variable1 == 't') || (variable1 == 'T'))
	   if ((variable2 == 'b') && (variable2 == 'B'))
	   
	   Die zwei || stehehn hierbei für oder.
	   Die zwei && stehen hierbei für und.
	   
	   */

	return 0;
}
